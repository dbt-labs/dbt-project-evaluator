version: 2

seeds:
  - name: test_fct_untested_models
    config:
      column_types:
        UNIQUE_ID: varchar
        TESTS_PER_MODEL: int
    tests:
      - dbt_utils.equality:
          compare_model: ref('fct_untested_models')
          compare_columns:
            - unique_id
            - tests_per_model