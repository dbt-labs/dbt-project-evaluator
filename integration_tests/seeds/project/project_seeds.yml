version: 2

seeds:

  - name: test_fct_unidentified_models
    config:
      column_types:
        RESOURCE_ID: varchar
        RESOURCE_NAME: varchar
        FILE_PATH: varchar
    tests:
      - dbt_utils.equality:
          compare_model: ref('fct_unidentified_models')
          compare_columns:
            - resource_id
            - resource_name
            - file_path
