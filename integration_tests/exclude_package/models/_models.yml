models:
  - name: excluded_model
    access: public
    config: 
      contract: 
        enforced: true
    columns: 
      - name: id
        data_type: integer
      - name: data_quality
        data_type: "{{ 'string' if target.type in ['bigquery', 'databricks'] else 'text' }}"