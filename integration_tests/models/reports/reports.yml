version: 2

models:
  - name: report_1
    description: tom cruise's minorty report
    access: public
  - name: report_2
    access: public
    config:
      materialized: table
      contract:
        enforced: "{{ true if target.type not in ['databricks'] else false }}"
    description: the colbert report
    columns:
      - name: id 
        data_type: integer