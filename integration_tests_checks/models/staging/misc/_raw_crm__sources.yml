sources:
  - name: raw_crm  # undocumented source, no freshness, yaml not in a raw_crm directory
    schema: raw_crm
    tables:
      - name: contacts  # undocumented source table
      - name: accounts
  - name: raw_shop_copy  # duplicates raw_shop.orders
    schema: raw_shop
    tables:
      - name: orders
