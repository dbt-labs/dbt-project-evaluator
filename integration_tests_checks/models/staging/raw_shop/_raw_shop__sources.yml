sources:
  - name: raw_shop
    description: Raw shop data.
    schema: raw_shop
    config:
      freshness:
        warn_after: {count: 24, period: hour}
      loaded_at_field: _loaded_at
    tables:
      - name: orders
        description: Raw orders.
      - name: customers
        description: Raw customers.
      - name: unused_table
        description: Nothing selects from this.  # fct_unused_sources
