version: 2 

  - name: fct_staging_directories
    description: >
      This finds all cases where a staging model or source definition is NOT in the appropriate subdirectory.
    tests:
      - is_empty